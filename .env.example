# Spotify MCP Server Configuration
# Copy this file to .env and fill in your Spotify app credentials

# Environment (affects security validation)
NODE_ENV=development

# Spotify App Credentials (from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Redirect URI - MUST match exactly what's configured in Spotify Dashboard
# Development: HTTP localhost is allowed
SPOTIFY_REDIRECT_URI=http://localhost:8080/callback
# Staging: HTTPS recommended
# SPOTIFY_REDIRECT_URI=https://staging.yourdomain.com/callback
# Production: HTTPS required (see docs/REDIRECT_URI_SECURITY.md)
# SPOTIFY_REDIRECT_URI=https://yourdomain.com/callback

# Optional: Allowed domains for redirect URI validation (comma-separated)
# SPOTIFY_ALLOWED_DOMAINS=localhost,127.0.0.1,yourdomain.com

# Logging Configuration
LOG_LEVEL=info

# Optional: Custom callback server port (default: 8080)
# CALLBACK_PORT=8080

# Security Configuration
# REQUIRE_HARDWARE_HSM=false